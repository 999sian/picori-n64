diff --git a/include/entity.h b/include/entity.h
index 887757c9..ba762835 100644
--- a/include/entity.h
+++ b/include/entity.h
@@ -208,30 +208,59 @@ typedef struct Entity_ {
     /*0x16*/ u8 carryFlags;     /**< Flags for carrying this Entity. */
     /*0x17*/ u8 followerFlag;   /**< Controls collisions between followers, unused. */
     /*0x18*/ struct PACKED SpriteSettings {
+    /* #N64 BE: reversed field order so draw stays bits 0-1 (raw & 3) as on LE/GBA. */
+#if __BYTE_ORDER__ == __ORDER_BIG_ENDIAN__
+    /*    */     u8 flipY       : 1; /* 0x80 */
+    /*    */     u8 flipX       : 1; /* 0x40 */
+    /*    */     u8 shadow      : 2; /* 0x10-0x20 */
+    /*    */     u8 ss3         : 1; /*   8 */
+    /*    */     u8 ss2         : 1; /*   4 */
+    /*    */     u8 draw        : 2; /* 0x2 */
+#else
     /*    */     u8 draw        : 2; /**< Draw type. 0 = disabled, 1 = clip to screen, 3 = draw always */ /* 0x2 */ 
     /*    */     u8 ss2         : 1; /*   4 */
     /*    */     u8 ss3         : 1; /*   8 */
     /*    */     u8 shadow      : 2; /* 0x10-0x20 */
     /*    */     u8 flipX       : 1; /**< Flip sprite horizontally. */ /* 0x40 */ 
     /*    */     u8 flipY       : 1; /**< Flip sprite vertically. */ /* 0x80 */ 
+#endif
     /*    */ } spriteSettings;
     /*0x19*/ struct PACKED SpriteRendering {
+#if __BYTE_ORDER__ == __ORDER_BIG_ENDIAN__
+    /*    */     u8 b3         : 2; /* 0x40 */
+    /*    */     u8 b2         : 2; /* 0x10 */
+    /*    */     u8 alphaBlend : 2; /* 4-8 */
+    /*    */     u8 b0         : 2; /* 1-2 */
+#else
     /*    */     u8 b0         : 2; /* 1-2 */
     /*    */     u8 alphaBlend : 2; /* 4-8 */
     /*    */     u8 b2         : 2; /* 0x10 */
     /*    */     u8 b3         : 2; /* 0x40 */
+#endif
     /*    */ } spriteRendering;
     /*0x1a*/ union PACKED Palette {
     /*    */    u8 raw;
     /*    */    struct PACKED PaletteB {
+    /*    */        /* #N64 BE: reversed so .b0 stays the LOW nibble (raw & 0xF), as on LE. Verified via MIPS disasm. */
+#if __BYTE_ORDER__ == __ORDER_BIG_ENDIAN__
+    /*    */        u8 b4 : 4;
+    /*    */        u8 b0 : 4;
+#else
     /*    */        u8 b0 : 4;
     /*    */        u8 b4 : 4;
+#endif
     /*    */    } b;
     /*    */ } palette;
     /*0x1b*/ struct PACKED SpriteOrientation {
+#if __BYTE_ORDER__ == __ORDER_BIG_ENDIAN__
+    /*    */     u8 flipY : 2; /* 0x20-0x40 */
+    /*    */     u8 b1    : 5; /* 0x2-0x10 */
+    /*    */     u8 b0    : 1; /* 1  */
+#else
     /*    */     u8 b0    : 1; /* 1  */
     /*    */     u8 b1    : 5; /* 0x2-0x10 */
     /*    */     u8 flipY : 2; /* 0x20-0x40 */
+#endif
     /*    */ } spriteOrientation;
     /*0x1c*/ u8 gustJarFlags; /**< Controls sfx and other things */
     /*0x1d*/ u8 gustJarTolerance; /**< Frames needed to pull off ground. */
diff --git a/include/gba/types.h b/include/gba/types.h
index 38540405..c0150f45 100644
--- a/include/gba/types.h
+++ b/include/gba/types.h
@@ -5,11 +5,23 @@
 
 typedef uint8_t u8;
 typedef uint16_t u16;
+#ifdef TMC_N64
+/* mips64-newlib makes uint32_t 'unsigned long' — a type distinct from 'unsigned int'
+ * and from enum underlying types, which breaks function-prototype/enum compatibility
+ * the decomp relies on (it holds on x86 where uint32_t == unsigned int). Use plain
+ * 'unsigned int' on N64: still 32-bit under o64, and matches the x86 type identity. */
+typedef unsigned int u32;
+#else
 typedef uint32_t u32;
+#endif
 typedef uint64_t u64;
 typedef int8_t s8;
 typedef int16_t s16;
+#ifdef TMC_N64
+typedef int s32;
+#else
 typedef int32_t s32;
+#endif
 typedef int64_t s64;
 
 typedef volatile u8 vu8;
diff --git a/include/global.h b/include/global.h
index f4a6cc2b..cbb15d68 100644
--- a/include/global.h
+++ b/include/global.h
@@ -124,7 +124,11 @@
 typedef union {
     s32 WORD;
     struct {
+#if __BYTE_ORDER__ == __ORDER_BIG_ENDIAN__
+        s16 y, x; /* #N64 BE: reversed so .x/.y alias the same WORD bits as little-endian */
+#else
         s16 x, y;
+#endif
     } HALF;
 } Coords;
 
@@ -136,17 +140,37 @@ typedef struct {
 union SplitDWord {
     s64 DWORD;
     u64 DWORD_U;
+#if __BYTE_ORDER__ == __ORDER_BIG_ENDIAN__
+    struct {
+        s32 HI, LO;
+    } HALF;
+    struct {
+        u32 HI, LO;
+    } HALF_U;
+#else
     struct {
         s32 LO, HI;
     } HALF;
     struct {
         u32 LO, HI;
     } HALF_U;
+#endif
 };
 
 union SplitWord {
     s32 WORD;
     u32 WORD_U;
+#if __BYTE_ORDER__ == __ORDER_BIG_ENDIAN__
+    struct {
+        s16 HI, LO;
+    } HALF;
+    struct {
+        u16 HI, LO;
+    } HALF_U;
+    struct {
+        u8 byte3, byte2, byte1, byte0;
+    } BYTES;
+#else
     struct {
         s16 LO, HI;
     } HALF;
@@ -156,13 +180,20 @@ union SplitWord {
     struct {
         u8 byte0, byte1, byte2, byte3;
     } BYTES;
+#endif
 };
 
 union SplitHWord {
     u16 HWORD;
+#if __BYTE_ORDER__ == __ORDER_BIG_ENDIAN__
+    struct {
+        u8 HI, LO;
+    } PACKED HALF;
+#else
     struct {
         u8 LO, HI;
     } PACKED HALF;
+#endif
 } PACKED;
 
 #if defined(_MSC_VER) && !defined(__clang__) && !defined(__GNUC__)
diff --git a/port/port_audio.h b/port/port_audio.h
index 3b9ab661..066ae9b4 100644
--- a/port/port_audio.h
+++ b/port/port_audio.h
@@ -3,7 +3,9 @@
 
 #include <stdbool.h>
 #include <stdint.h>
+#ifndef TMC_N64
 #include <SDL3/SDL.h>
+#endif
 
 bool Port_Audio_Init(void);
 void Port_Audio_Shutdown(void);
diff --git a/port/port_draw.c b/port/port_draw.c
index 2f3b0df3..1a524262 100644
--- a/port/port_draw.c
+++ b/port/port_draw.c
@@ -54,6 +54,16 @@ static inline u32 ReadU32Unaligned(const void* p) {
     return v;
 }
 
+#ifdef TMC_N64
+/* gFrameObjLists is a flat blob of little-endian u32 self-relative offsets
+ * (GBA ROM byte order). The frame *data* it points at is read byte-wise, so the
+ * blob must stay in LE byte order — only the u32 offset reads need LE decode. */
+static inline u32 ReadU32LE(const void* p) {
+    const u8* b = (const u8*)p;
+    return (u32)b[0] | ((u32)b[1] << 8) | ((u32)b[2] << 16) | ((u32)b[3] << 24);
+}
+#endif
+
 /* ---- Struct layout diagnostic (one-time) ---- */
 static void Port_CheckStructLayouts(void) {
     static int done = 0;
@@ -302,7 +312,11 @@ static const u8* LookupFrameData(u16 spriteIndex, u8 frameIndex) {
         return NULL;
     }
 
+#ifdef TMC_N64
+    u32 off1 = ReadU32LE(base + (size_t)spriteIndex * sizeof(u32));
+#else
     u32 off1 = gFrameObjLists[spriteIndex];
+#endif
     if ((size_t)off1 > frameObjSize - sizeof(u32)) {
         return NULL;
     }
@@ -312,7 +326,11 @@ static const u8* LookupFrameData(u16 spriteIndex, u8 frameIndex) {
         return NULL;
     }
 
+#ifdef TMC_N64
+    u32 off2 = ReadU32LE(base + frameEntry);
+#else
     u32 off2 = ReadU32Unaligned(base + frameEntry);
+#endif
     if ((size_t)off2 >= frameObjSize) {
         return NULL;
     }
@@ -437,13 +455,23 @@ static void RenderSpritePieces(const u8* data, /* pointer to frame data (count b
          *   bits 25-29: attr1.matrixNum/flip (from 'flags')
          *   bits 30-31: attr1.size
          */
+#ifdef TMC_N64
+        /* #N64 BE: ViruaPPU reads each OAM attr as a native u16 (cf. the attr2
+         * store below + the viruappu-n64 patch). The combined-u32 memcpy lands
+         * attr1 where attr0 belongs on big-endian (X/Y swap -> sprites collapse
+         * to a vertical strip), so pack attr0/attr1 as separate native u16s. */
+        u16 _attr0 = (u16)((u32)(y & 0xFF) | (flags & 0xFFFFu) | ((u32)(shapeInfo & 0xC0) << 8));
+        u16 _attr1 = (u16)(((u32)(x & 0x1FF) | (flags >> 16)) ^ ((u32)(shapeInfo & 0x3C) << 10));
+        memcpy(ip + 0, &_attr0, sizeof(_attr0));
+        memcpy(ip + 2, &_attr1, sizeof(_attr1));
+#else
         u32 oamWord = (u32)(y & 0xFF);            /* y position */
         oamWord |= (u32)((x & 0x1FF)) << 16;      /* x position */
         oamWord |= flags;                         /* base flags */
         oamWord |= (u32)(shapeInfo & 0xC0) << 8;  /* shape → attr0 bits 14-15 */
         oamWord ^= (u32)(shapeInfo & 0x3C) << 26; /* flip/size → attr1 bits 12-15 */
-
         memcpy(ip, &oamWord, sizeof(oamWord));
+#endif
         ip += 4;
 
         /* Build attr2: tile number + priority + palette */
diff --git a/port/port_gameplay_stubs.c b/port/port_gameplay_stubs.c
index 1f2ad7b9..eae73595 100644
--- a/port/port_gameplay_stubs.c
+++ b/port/port_gameplay_stubs.c
@@ -22,7 +22,7 @@
  * on Windows. The cutscene-watchdog fix doesn't depend on it — it's
  * only used to identify the call path that deletes orchestrator
  * entities during #93 debug. */
-#if !defined(_WIN32) && !defined(__MINGW32__) && !defined(__MINGW64__)
+#if !defined(_WIN32) && !defined(__MINGW32__) && !defined(__MINGW64__) && !defined(TMC_N64)
 #include <execinfo.h>
 #define PORT_HAVE_EXECINFO 1
 #else
diff --git a/port/port_gba_mem.h b/port/port_gba_mem.h
index fb8ee35b..b00f5b1a 100644
--- a/port/port_gba_mem.h
+++ b/port/port_gba_mem.h
@@ -56,10 +56,26 @@ static inline void* gba_TryMemPtr(uint32_t addr) {
     return NULL;
 }
 
+#ifdef TMC_N64
+extern volatile uint32_t g_n64_last_bad_addr;
+extern volatile uint32_t g_n64_bad_count;
+#endif
+
 static inline void* gba_MemPtr(uint32_t addr) {
     void* ptr = gba_TryMemPtr(addr);
     if (ptr)
         return ptr;
+#ifdef TMC_N64
+    /* #N64: never abort on a stray GBA address (libdragon abort() draws the
+     * CPU-exception inspector and halts). Record it (surfaced on-screen by
+     * Port_N64_VBlank) and return a shared scratch page so the engine limps. */
+    {
+        static u8 sN64Scratch[0x10000];
+        g_n64_last_bad_addr = addr;
+        g_n64_bad_count++;
+        return sN64Scratch;
+    }
+#else
 #if defined(__GNUC__)
     void* caller = __builtin_return_address(0);
     fprintf(stderr, "FATAL: gba_MemPtr: invalid address 0x%08X (called from %p)\n", addr, caller);
@@ -77,6 +93,7 @@ static inline void* gba_MemPtr(uint32_t addr) {
 #endif
     abort();
     return NULL;
+#endif
 }
 
 /*
diff --git a/port/port_rom.c b/port/port_rom.c
index e22c0803..bacf68ef 100644
--- a/port/port_rom.c
+++ b/port/port_rom.c
@@ -15,7 +15,9 @@
 #include "port_gba_mem.h"
 #include "structures.h"
 #include "tileMap.h"
+#ifndef TMC_N64
 #include <SDL3/SDL.h>
+#endif
 #include <stdio.h>
 #include <stdlib.h>
 #include <string.h>
@@ -30,7 +32,7 @@
 #include <mach-o/dyld.h>
 #include <stdlib.h>      /* realpath */
 #include <sys/stat.h>
-#else
+#elif !defined(TMC_N64)
 #include <dirent.h>
 #include <sys/stat.h>
 /* Forward-declare readlink to avoid pulling in _POSIX_C_SOURCE feature test
@@ -38,6 +40,20 @@
 extern long readlink(const char* path, char* buf, unsigned long bufsiz);
 #endif
 
+#ifdef TMC_N64
+/* N64/newlib lacks <dirent.h> and several POSIX FS calls. The host ROM
+ * file-finding functions that use them are never called on N64 (Port_LoadRom
+ * skips file I/O) and are dropped by --gc-sections, but must still COMPILE,
+ * so provide just enough declarations. */
+typedef struct __n64_DIR DIR;
+struct dirent { char d_name[256]; };
+DIR* opendir(const char*);
+struct dirent* readdir(DIR*);
+int closedir(DIR*);
+int mkdir(const char*, unsigned);
+extern long readlink(const char*, char*, unsigned long);
+#endif
+
 u8* gRomData = NULL;
 u32 gRomSize = 0;
 static SpritePtr sSpritePtrsStable[512];
@@ -76,8 +92,12 @@ static FILE* TryOpenRom(const char** paths, int count, char* foundPath, int foun
 static void FatalRomError(const char* title, const char* message) {
     fprintf(stderr, "ERROR: %s\n", message);
     fflush(stderr);
+#ifndef TMC_N64
     SDL_ShowSimpleMessageBox(SDL_MESSAGEBOX_ERROR, title, message, NULL);
     SDL_Quit();
+#else
+    (void)title;
+#endif
     exit(1);
 }
 
@@ -498,12 +518,26 @@ bool32 Port_IsAreaTablePtrReadable(u32 area, const void* ptr) {
 /* Forward declaration */
 static inline void* ResolveRomPtr(u32 gba_addr);
 
+/* Read a GBA little-endian u16/u32 from a ROM byte pointer, host-endian-safe.
+ * GBA ROM data is little-endian; a native u16/u32 load byte-swaps it on a
+ * big-endian host (N64). Reading byte-by-byte in LE order is correct on both
+ * little-endian PC and big-endian N64. */
+static inline u32 RomLE32(const void* p) {
+    /* Use a 32-bit load: N64 CPU byte reads (lbu) from the cart are corrupted
+     * (odd 16-bit halfwords dropped), but 32-bit loads (lw) work. The lw returns
+     * the GBA little-endian bytes in big-endian order on N64, so byte-swap. */
+    u32 v = *(const u32*)p;
+#ifdef TMC_N64
+    v = __builtin_bswap32(v);
+#endif
+    return v;
+}
+
 /* Resolve a ROM sub-table of 32-bit GBA pointers into a native pointer array. */
 static void ResolveSubTable(void* romBase, void** dest, u32 count) {
     u8* base = (u8*)romBase;
     for (u32 j = 0; j < count; j++) {
-        u32 ptr;
-        memcpy(&ptr, base + j * 4, 4);
+        u32 ptr = RomLE32(base + j * 4);
         dest[j] = ResolveRomPtr(ptr);
     }
 }
@@ -517,8 +551,7 @@ static u32 ScanSubArrayCount(const void* base) {
     }
 
     for (u32 i = 0; i < MAX_ROOMS; i++) {
-        u32 value;
-        memcpy(&value, bytes + i * 4, 4);
+        u32 value = RomLE32(bytes + i * 4);
         if (value == 0 || (value >= 0x08000000u && value < 0x08000000u + gRomSize)) {
             count = i + 1;
         } else {
@@ -601,6 +634,10 @@ extern struct_020227E8 gUnk_020227E8[];
 
 /* Resolved pointer tables */
 const u8* gGlobalGfxAndPalettes = NULL;
+/* #N64: cart base of the map/gfx blob. gMapData is only a 1 MB RAM placeholder on
+ * N64 but the engine indexes the full ~13 MB blob, so LoadMapData reads src straight
+ * from the cart (gRomData) here and lets the LZ77/DMA path stream it. */
+const u8* gMapDataRomSrc = NULL;
 const void* gGfxGroups[GFX_GROUPS_COUNT_MAX];
 const void* gPaletteGroups[PALETTE_GROUPS_COUNT_MAX];
 
@@ -636,8 +673,7 @@ void* Port_ReadPackedRomPtr(const void* base, u32 index) {
     /* For PC `.rodata` stubs we have no size info — trust the caller. */
 
     const u8* readPtr = (const u8*)base + index * 4;
-    u32 raw;
-    memcpy(&raw, readPtr, 4);
+    u32 raw = RomLE32(readPtr);
     if (raw == 0)
         return NULL;
     raw &= ~1u;
@@ -670,7 +706,7 @@ void* Port_ResolveEwramPtr(u32 gba_addr) {
              * PC  layout:  bgSettings(8)  mapData(0x2000)  collisionData(0x1000) ...
              * All fields after bgSettings are shifted by +4 on PC.
              */
-            u32 pc_off = (gba_off < 4) ? gba_off : gba_off + 4;
+            u32 pc_off = (gba_off < 4) ? gba_off : gba_off + ((__SIZEOF_POINTER__ == 8) ? 4u : 0u);
             return (u8*)&gMapBottom + pc_off;
         }
     }
@@ -680,7 +716,7 @@ void* Port_ResolveEwramPtr(u32 gba_addr) {
         const u32 GBA_SIZE = 0xC010u;
         if (gba_addr >= GBA_BASE && gba_addr < GBA_BASE + GBA_SIZE) {
             u32 gba_off = gba_addr - GBA_BASE;
-            u32 pc_off = (gba_off < 4) ? gba_off : gba_off + 4;
+            u32 pc_off = (gba_off < 4) ? gba_off : gba_off + ((__SIZEOF_POINTER__ == 8) ? 4u : 0u);
             return (u8*)&gMapTop + pc_off;
         }
     }
@@ -933,6 +969,7 @@ static int LoadRomGaps(void) {
 }
 
 void Port_LoadRom(const char* path) {
+#ifndef TMC_N64
     memset(sExtractedPages, 0, sizeof(sExtractedPages));
 
     /* ---- Step 1: try loading from rom_data/ extracted pages ---- */
@@ -1029,6 +1066,14 @@ void Port_LoadRom(const char* path) {
             "No ROM data available after loading.\n\n"
             "The ROM file may be empty or unreadable.");
     }
+#else
+    /* #N64: gRomData/gRomSize already point at the embedded cart ROM (set by
+     * n64_main before calling Port_LoadRom). Skip all host file I/O. */
+    (void)path;
+    if (!gRomData || gRomSize == 0) {
+        return;
+    }
+#endif
 
     /* ---- Step 3: auto-detect ROM region ---- */
     Port_DetectRomRegion(gRomData, gRomSize);
@@ -1037,11 +1082,36 @@ void Port_LoadRom(const char* path) {
     fprintf(stderr, "Using offsets for %s (game code: %.4s)\n", gRomRegion == ROM_REGION_EU ? "EU" : "USA",
             R->gameCode);
 
+#ifdef TMC_N64
+    {   /* Byte-order / alignment probe: compare against known GBA header bytes.
+         * Logo@0x04 is the fixed sequence 24 FF AE 51 69 9A A2 21; gamecode@0xAC
+         * is 'BZME' = 42 5A 4D 45. Mismatch => cart CPU byte reads are swapped or
+         * gRomData is misaligned. */
+        const u8* h = gRomData;
+        fprintf(stderr, "[romdump] hdr[0..7]=%02x %02x %02x %02x %02x %02x %02x %02x  logo[4..11]=%02x %02x %02x %02x %02x %02x %02x %02x (expect 24 FF AE 51 69 9A A2 21)\n",
+                h[0],h[1],h[2],h[3],h[4],h[5],h[6],h[7], h[4],h[5],h[6],h[7],h[8],h[9],h[10],h[11]);
+        fprintf(stderr, "[romdump] gamecode[AC..AF]=%02x %02x %02x %02x (expect 42 5A 4D 45 'BZME')\n",
+                h[0xAC],h[0xAD],h[0xAE],h[0xAF]);
+        const u8* g = &gRomData[R->gfxGroups];
+        fprintf(stderr, "[romdump] gfxGroups@0x%X [0..11]=%02x %02x %02x %02x %02x %02x %02x %02x %02x %02x %02x %02x\n",
+                R->gfxGroups, g[0],g[1],g[2],g[3],g[4],g[5],g[6],g[7],g[8],g[9],g[10],g[11]);
+    }
+#endif
+
     /* ---- Step 4: resolve ROM symbols using compile-time tables + gRomData ---- */
 
     /* gGlobalGfxAndPalettes — huge palette/gfx blob (still points into gRomData) */
     gGlobalGfxAndPalettes = &gRomData[R->gfxAndPalettes];
 
+    /* gGfxGroups / gPaletteGroups — resolve the ROM pointer tables (arrays of
+     * GBA pointers to GfxItem / PaletteGroup descriptors). On PC these come from
+     * the asset pipeline; on N64 (assets stubbed) they MUST be resolved from the
+     * ROM here, else LoadGfxGroup/LoadPaletteGroup see NULL and skip every load. */
+    ResolveSubTable(&gRomData[R->gfxGroups], (void**)gGfxGroups, R->gfxGroupsCount);
+    ResolveSubTable(&gRomData[R->paletteGroups], (void**)gPaletteGroups, R->paletteGroupsCount);
+    fprintf(stderr, "gGfxGroups/gPaletteGroups resolved (%u gfx, %u palette) from ROM.\n",
+            R->gfxGroupsCount, R->paletteGroupsCount);
+
     /* gPalette_549 — Mt Crenel weather-change reads a 26-palette contiguous
      * block starting here. Copy it out of gGlobalGfxAndPalettes (offset 0x44A0
      * — same relative position on USA and EU because the palette ordering in
@@ -1089,7 +1159,18 @@ void Port_LoadRom(const char* path) {
     /* OBJ palette offset table — now compile-time const from src/data/objPalettes.c */
 
     /* gFixedTypeGfxData — from compile-time const data */
+#ifdef TMC_N64
+    /* ROM stores these as little-endian u32 (count<<24 | tile-src offset). The
+     * array is read natively (gFixedTypeGfxData[i]), so on big-endian N64 each
+     * word must be LE-decoded or count comes out garbage and LoadFixedGFX fails. */
+    for (u32 _fi = 0; _fi < R->fixedTypeGfxCount; _fi++) {
+        const u8* _fb = &kFixedTypeGfxInitData[_fi * 4];
+        gFixedTypeGfxData[_fi] =
+            (u32)_fb[0] | ((u32)_fb[1] << 8) | ((u32)_fb[2] << 16) | ((u32)_fb[3] << 24);
+    }
+#else
     memcpy(gFixedTypeGfxData, kFixedTypeGfxInitData, R->fixedTypeGfxCount * 4);
+#endif
     fprintf(stderr, "gFixedTypeGfxData loaded (%u entries from compile-time table).\n", R->fixedTypeGfxCount);
 
     /* gSpritePtrs — resolved from compile-time offset table, then
@@ -1171,7 +1252,11 @@ void Port_LoadRom(const char* path) {
                 u32 resolvedCount = 0;
                 for (u32 i = 0; i < SPRITE_ANIM_322_COUNT; i++) {
                     u32 gbaPtr;
+#ifdef TMC_N64
+                    gbaPtr = RomLE32(animTable + i * 4); /* raw memcpy reads byte-swapped on BE N64 */
+#else
                     memcpy(&gbaPtr, animTable + i * 4, 4);
+#endif
                     if (gbaPtr == 0) {
                         break;
                     }
@@ -1257,7 +1342,12 @@ void Port_LoadRom(const char* path) {
         u32 mapDataSize = gRomSize - R->mapDataBase;
         if (mapDataSize > 0xE00000u)
             mapDataSize = 0xE00000u;
+#ifdef TMC_N64
+        if (mapDataSize > 0x100000u)
+            mapDataSize = 0x100000u; /* gMapData is a 1 MB placeholder on N64 */
+#endif
         memcpy(gMapData, &gRomData[R->mapDataBase], mapDataSize);
+        gMapDataRomSrc = &gRomData[R->mapDataBase];
         fprintf(stderr, "gMapData loaded (%u bytes from ROM offset 0x%X).\n", mapDataSize, R->mapDataBase);
     }
 
diff --git a/port/port_types.h b/port/port_types.h
index ba38afbd..328632ca 100644
--- a/port/port_types.h
+++ b/port/port_types.h
@@ -12,12 +12,20 @@
 // Type definitions for GBA-style fixed-width types
 typedef uint8_t u8;
 typedef uint16_t u16;
+#ifdef TMC_N64
+typedef unsigned int u32; /* match gba/types.h: mips-newlib uint32_t is 'unsigned long' */
+#else
 typedef uint32_t u32;
+#endif
 typedef uint64_t u64;
 
 typedef int8_t s8;
 typedef int16_t s16;
+#ifdef TMC_N64
+typedef int s32;
+#else
 typedef int32_t s32;
+#endif
 typedef int64_t s64;
 
 typedef volatile u8 vu8;
diff --git a/src/beanstalkSubtask.c b/src/beanstalkSubtask.c
index 61cff0c0..0cc349d4 100644
--- a/src/beanstalkSubtask.c
+++ b/src/beanstalkSubtask.c
@@ -184,7 +184,6 @@ void LoadMapData(MapDataDefinition* dataDefinition) {
             u32 field_src = Port_ReadU32(raw + 0);
             u32 field_dest_gba = Port_ReadU32(raw + 4);
             u32 field_size = Port_ReadU32(raw + 8);
-
             if (field_dest_gba != 0) {
                 dest = Port_ResolveEwramPtr(field_dest_gba);
                 if (!dest) {
@@ -192,7 +191,15 @@ void LoadMapData(MapDataDefinition* dataDefinition) {
                     raw += 12;
                     continue;
                 }
+#ifdef TMC_N64
+                {   /* #N64: read from the cart blob, not the 1 MB gMapData placeholder
+                     * (the engine indexes past 1 MB; LZ77/DMA streams it from cart). */
+                    extern const u8* gMapDataRomSrc;
+                    src = (u8*)gMapDataRomSrc + (field_src & 0x7fffffff);
+                }
+#else
                 src = (u8*)&gMapData + (field_src & 0x7fffffff);
+#endif
                 if ((field_size & MAP_COMPRESSED) != 0) {
                     if ((field_dest_gba >> 0x18) == 6) {
                         LZ77UnCompVram(src, dest);
diff --git a/src/entity.c b/src/entity.c
index 53e16694..6d50cbf9 100644
--- a/src/entity.c
+++ b/src/entity.c
@@ -673,16 +673,27 @@ void ClearAllDeletedEntities(void) {
 #ifdef PC_PORT
     /* On PC, gPlayerEntity/gAuxPlayerEntities/gEntities are separate globals. */
     int i;
-    if ((intptr_t)gPlayerEntity.base.prev < 0)
+    /* #N64: every KSEG0 pointer is 0x8xxxxxxx, i.e. negative as intptr_t, so the
+     * GBA/PC "deleted = prev<0" proxy flags *every live entity* as deleted and
+     * ClearDeletedEntity zeroes the whole pool each frame (title objects, Link,
+     * everything). Test the actual deletion sentinel (prev == (Entity*)-1) on N64.
+     * The PC build keeps the original `< 0` form verbatim (byte-identical). */
+#ifdef TMC_N64
+#define ENT_DELETED_PREV(p) ((uintptr_t)(void*)(p) == (uintptr_t)(intptr_t)-1)
+#else
+#define ENT_DELETED_PREV(p) ((intptr_t)(p) < 0)
+#endif
+    if (ENT_DELETED_PREV(gPlayerEntity.base.prev))
         ClearDeletedEntity(&gPlayerEntity.base);
     for (i = 0; i < MAX_AUX_PLAYER_ENTITIES; i++) {
-        if ((intptr_t)gAuxPlayerEntities[i].base.prev < 0)
+        if (ENT_DELETED_PREV(gAuxPlayerEntities[i].base.prev))
             ClearDeletedEntity(&gAuxPlayerEntities[i].base);
     }
     for (i = 0; i < MAX_ENTITIES; i++) {
-        if ((intptr_t)gEntities[i].base.prev < 0)
+        if (ENT_DELETED_PREV(gEntities[i].base.prev))
             ClearDeletedEntity(&gEntities[i].base);
     }
+#undef ENT_DELETED_PREV
 #else
     GenericEntity* ent = (GenericEntity*)&gPlayerEntity.base;
     do {
diff --git a/src/fade.c b/src/fade.c
index b67ca34c..ad83a45d 100644
--- a/src/fade.c
+++ b/src/fade.c
@@ -42,6 +42,17 @@ static const u32 sFadeTableOffsets[NUM_FADE_BRIGHTNESS][3] = {
     { 0x1104, 0x1144, 0x1184 }, /* brightness 2 */
 };
 
+#ifdef TMC_N64
+/* 16-bit cart reads (lhu) are broken on N64's strict PI bus; read the containing
+ * 32-bit-aligned word (lw is fine) and extract the halfword (big-endian: the
+ * lower address is the high half). Returns the same value the GBA's lhu would. */
+static inline u16 n64_cart_u16(const void* p) {
+    uintptr_t a = (uintptr_t)p;
+    u32 w = *(const volatile u32*)(void*)(a & ~(uintptr_t)3);
+    return (a & 2u) ? (u16)(w & 0xFFFFu) : (u16)(w >> 16);
+}
+#endif
+
 static void Port_MakeFadeBuff256(u8* src, u8* dest, u16 intensity, u8 color) {
     u32 bias = (u32)intensity * (u32)color;
     u32 factor = 0x400 - (u32)intensity * 4;
@@ -64,6 +75,25 @@ static void Port_MakeFadeBuff256(u8* src, u8* dest, u16 intensity, u8 color) {
     if (!dstPtr)
         return; // Safety check
 
+#ifdef TMC_N64
+    /* The fade tables live in the cart; the tableR[r>>1] loads below are 16-bit
+     * cart reads (lhu) that return garbage on N64's strict PI bus -> wrong faded
+     * colors every frame (the ares "wrong colours" bug). Cache the 3 tables
+     * (32 u16 each) in RAM via 32-bit-safe reads, re-copied only when brightness
+     * (hence the table set) changes, then index the RAM copies. */
+    static u16 sCacheR[32], sCacheG[32], sCacheB[32];
+    static int sCacheBrightness = -1;
+    if ((int)brightness != sCacheBrightness) {
+        for (int k = 0; k < 32; k++) {
+            sCacheR[k] = n64_cart_u16(&tableR[k]);
+            sCacheG[k] = n64_cart_u16(&tableG[k]);
+            sCacheB[k] = n64_cart_u16(&tableB[k]);
+        }
+        sCacheBrightness = (int)brightness;
+    }
+    tableR = sCacheR; tableG = sCacheG; tableB = sCacheB;
+#endif
+
     for (int i = 0; i < 16; i++) {
         u16 col = srcPtr[i];
         u32 shifted = (u32)col << 1;
diff --git a/src/gameUtils.c b/src/gameUtils.c
index 215c24f8..1a6437ee 100644
--- a/src/gameUtils.c
+++ b/src/gameUtils.c
@@ -699,6 +699,15 @@ void InitAllRoomResInfo(void) {
 }
 
 void InitRoomResInfo(RoomResInfo* info, RoomHeader* r_hdr, u32 area, u32 room) {
+#ifdef TMC_N64
+    RoomHeader n64hdr = *r_hdr;   /* #N64: swap LE ROM u16 fields (else wrong tileSet_id -> black room) */
+    n64hdr.map_x        = __builtin_bswap16(n64hdr.map_x);
+    n64hdr.map_y        = __builtin_bswap16(n64hdr.map_y);
+    n64hdr.pixel_width  = __builtin_bswap16(n64hdr.pixel_width);
+    n64hdr.pixel_height = __builtin_bswap16(n64hdr.pixel_height);
+    n64hdr.tileSet_id   = __builtin_bswap16(n64hdr.tileSet_id);
+    r_hdr = &n64hdr;
+#endif
     info->map_x = r_hdr->map_x;
     info->map_y = r_hdr->map_y;
     info->pixel_width = r_hdr->pixel_width;
@@ -821,6 +830,17 @@ void sub_08052FF4(u32 area, u32 room) {
 #endif
         return;
     }
+#ifdef TMC_N64
+    /* #N64: RoomHeader is little-endian ROM data; its u16 fields read byte-swapped on the
+     * big-endian N64 — a wrong tileSet_id loads no room tiles (black room). Swap a local copy. */
+    RoomHeader n64hdr = *r_hdr;
+    n64hdr.map_x        = __builtin_bswap16(n64hdr.map_x);
+    n64hdr.map_y        = __builtin_bswap16(n64hdr.map_y);
+    n64hdr.pixel_width  = __builtin_bswap16(n64hdr.pixel_width);
+    n64hdr.pixel_height = __builtin_bswap16(n64hdr.pixel_height);
+    n64hdr.tileSet_id   = __builtin_bswap16(n64hdr.tileSet_id);
+    r_hdr = &n64hdr;
+#endif
 
     gArea.currentRoomInfo.map_x = r_hdr->map_x;
     gArea.currentRoomInfo.map_y = r_hdr->map_y;
diff --git a/src/room.c b/src/room.c
index f546ec3b..4c276982 100644
--- a/src/room.c
+++ b/src/room.c
@@ -72,6 +72,19 @@ Entity* LoadRoomEntity(const EntityData* dat) {
 #ifndef NON_MATCHING
     asm("" ::: "r5");
 #endif
+#ifdef TMC_N64
+    /* #N64: EntityData lives in little-endian ROM; its multi-byte fields read
+     * byte-swapped on the big-endian N64 (dat->spritePtr came back as 0x14DD0F08
+     * = bswap of 0x080FDD14, crashing room load). Swap into a local copy used both
+     * for the reads below and for the MemCopy into the entity (read back natively).
+     * The u8 fields (kind/flags/id/type) are unaffected. */
+    EntityData n64sw = *dat;
+    n64sw.type2     = __builtin_bswap32(n64sw.type2);
+    n64sw.xPos      = __builtin_bswap16(n64sw.xPos);
+    n64sw.yPos      = __builtin_bswap16(n64sw.yPos);
+    n64sw.spritePtr = __builtin_bswap32(n64sw.spritePtr);
+    dat = &n64sw;
+#endif
 
     kind = dat->kind & 0xF;
     if ((dat->flags & 0xF0) == 0x50 && DeepFindEntityByID(kind, dat->id))
@@ -329,6 +342,16 @@ static bool32 IsRoomPropertyListInRom(void** properties) {
 
 void sub_0804AFB0(void** properties) {
     u32 i;
+#ifdef TMC_N64
+    /* #N64: when the area/room property table doesn't resolve (ROM-struct endianness
+     * still being worked through the room-load chain), `properties` is NULL — guard the
+     * deref so room load continues (no entities) to BG rendering instead of crashing@0. */
+    if (properties == NULL) {
+        for (i = 0; i < 8; ++i) gRoomVars.properties[i] = NULL;
+        gCurrentRoomProperties = NULL;
+        return;
+    }
+#endif
 
     gCurrentRoomProperties = properties;
     for (i = 0; i < 8; ++i) {
diff --git a/src/title.c b/src/title.c
index f7b7813b..0d447183 100644
--- a/src/title.c
+++ b/src/title.c
@@ -79,7 +79,7 @@ static const u16 sLightRaysAlphaBlends[] = {
 #define FLAG_BYTE(bank, flag) (((bank) + (flag)) >> 3)
 #define FLAG_X(flag) (1 << ((flag) & 7))
 
-#ifdef DEMO_JP
+#if defined(DEMO_JP) || defined(TMC_N64) /* #N64: skip the (host-dep) file-select; demo save -> gameplay */
 static const SaveFile gDemoSave = {
     .initialized = 1,
     .msg_speed = 1,
@@ -113,6 +113,7 @@ static const SaveFile gDemoSave = {
         [22] = 0x40,
     },
     .flags = {
+#ifdef DEMO_JP /* #N64: USA flag enums aren't integer-constant array indices; N64 starts flags=0 */
         [FLAG_BYTE(FLAG_BANK_G, START)] = FLAG_X(START) | FLAG_X(EZERO_1ST) | FLAG_X(TABIDACHI),
         [FLAG_BYTE(FLAG_BANK_G, OUTDOOR)] = FLAG_X(OUTDOOR),
         [FLAG_BYTE(FLAG_BANK_G, ENTRANCE_0)] = FLAG_X(ENTRANCE_0),
@@ -126,6 +127,10 @@ static const SaveFile gDemoSave = {
         [FLAG_BYTE(FLAG_BANK_2, M_PRIEST_TALK)] = FLAG_X(M_PRIEST_TALK) | FLAG_X(M_ELDER_TALK1ST) | FLAG_X(M_PRIEST_MOVE),
         [FLAG_BYTE(FLAG_BANK_2, KOBITO_MORI_1ST)] = FLAG_X(KOBITO_MORI_1ST),
         [FLAG_BYTE(FLAG_BANK_5, LV1_0B_WALK)] = FLAG_X(LV1_0B_WALK),
+#endif
+#if defined(TMC_N64) && !defined(DEMO_JP)
+        [0] = 0,
+#endif
     },
 };
 static const u8 unk[] = { 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0x00, 0xd0,
@@ -463,7 +468,7 @@ static void HandleTitlescreenAnimationIntro(void) {
 }
 static void ExitTitlescreen(void) {
     if (!gFadeControl.active) {
-#ifdef DEMO_JP
+#if defined(DEMO_JP) || defined(TMC_N64)
         MemCopy(&gDemoSave, &gSave, sizeof(gSave));
         SetTask(TASK_GAME);
 #else
@@ -472,6 +477,16 @@ static void ExitTitlescreen(void) {
     }
 }
 
+#if defined(TMC_N64) && !defined(DEMO_JP)
+/* #N64: file-select is host-dependent and not yet compiled. The bring-up harness
+ * (port/n64/n64_main.c) calls this after a few title frames to drop straight into
+ * gameplay via the demo save, bypassing the title intro/input state machine. */
+void Port_N64_ForceGameStart(void) {
+    MemCopy(&gDemoSave, &gSave, sizeof(gSave));
+    SetTask(TASK_GAME);
+}
+#endif
+
 static u32 GetAdvanceState(void) {
     u32 newKeys;
 
